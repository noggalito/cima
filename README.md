# cima webapp

built using [ghost](https://ghost.org/)

## development

- install packages

  `$ npm install`

- launch server (and let ghost create the DB)

  `$ gulp`

- seed database

  `$ ./seed.rb`

## deployment

```
$ ./deploy.bash production
```
