{
  deploy : {
    production : {
      user : "macool",
      host : "produccion.cima.ec",
      ref  : "origin/production",
      repo : "git@github.com:noggalito/cima.git",
      path : "~/cima",
      "post-deploy" : "export PATH=~/.nvm/versions/v0.12.14/bin:$PATH && npm install --production && pm2 startOrRestart ~/cima/shared/ecosystem.json --env production"
    }
  }
}
